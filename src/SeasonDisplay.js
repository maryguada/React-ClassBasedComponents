import React from 'react'; 

const SeasonDisplay = () =>{
    return <div>SeasonDisplay!</div>;
};

// this is a child component, which mean we would need to 
// export.

// We import Season Display in parent component. 

export default SeasonDisplay; 